@model IEnumerable<GoGaming.Models.ComentarioViewModel>


@*<table class="table">
    @foreach (var item in Model)
    {
        <tr style="cursor: pointer;" onclick="window.location = '/Comentario/Details/'+@item.Id;">

            <td>
                @Html.ActionLink(item.nameAutor, "../Usuario/Details/", new { id = item.Autor })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contenido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hijos)
            </td>
        </tr>

    }
    @if ((int)Model.Count() == 0)
    {
        <div>No hay ningun comentario todavia</div>
    }

</table>*@
@*<table class="table">*@
@foreach (var item in Model)
{
    <div class="card w-50 jumbotron" style="background-color: #2C2F33;">
        <div style=" margin-top:0" onclick="window.location = '/Comentario/Details/'+@item.Id;">
            <h4 class="card-subtitle mb-2 text-muted"><i>@Html.ActionLink(item.nameAutor, "../Usuario/Details/", new { id = item.Autor })</i></h4>
            <h4 class="card-subtitle mb-2 text-muted">@Html.DisplayFor(modelItem => item.Hora)</h4>
            <p style="margin-top:1.5em; font-size: medium" ;>@Html.DisplayFor(modelItem => item.Contenido)</p>

        </div>
        @*<tr style="" onclick="window.location = '/Comentario/Details/'+@item.Id;">
                <td>

                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Contenido)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Hora)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Hijos)
                </td>
            </tr>*@
    </div>
}

@if ((int)Model.Count() == 0)
{
    <div class="card w-50 jumbotron"><p >No hay ningun comentario todavia</p></div>
}
@*</table>*@